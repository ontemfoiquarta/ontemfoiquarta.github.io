let aboutBtn = document.getElementById('about-btn')
let navBtns = document.getElementById('nav-buttons')

function moveAbout() {
    navBtns.style.position = 'relative'
    navBtns.style.top = 3230 + 'px';
}

function moveDesign() {
    navBtns.style.position = 'relative'
    navBtns.style.top = 1150 + 'px';
}

function moveHome() {
    navBtns.style.position = 'relative'
    navBtns.style.top = 40 + 'px';
}